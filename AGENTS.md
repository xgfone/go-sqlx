# 项目约定

## 文档与测试产物

- `docs/` 用于需要纳入 Git 版本管理的项目文档。放入其中的文件和目录，原则上都应提交到 Git。
- 测试数据和测试用文档原则上不要直接放入 `docs/`。需要随代码维护的测试夹具及配套文档放到相应包的 `testdata/` 目录。
- 临时测试数据、测试报告、基准测试输出、覆盖率及竞态检测结果等临时产物放到临时目录（如 `/tmp`），不要使用 `docs/` 存放不需要提交的内容。
